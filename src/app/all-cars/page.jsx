import React from 'react';

export const metadata = {
  title: "All Cars",
};

const AllCars = () => {
    return (
        <div>
            All Cars Page
        </div>
    );
};

export default AllCars;